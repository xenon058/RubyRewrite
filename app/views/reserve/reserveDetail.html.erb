<main id="main">
      <article>
  <!-- 各ページスクリプト挿入場所 -->
 <section>
    <h2>ご予約（詳細情報の入力）</h2>
    <p>詳細情報を入力後、予約確認ボタンを押してください。<br />
    （※がついている項目は入力必須項目です）</p>
    <h3>予約情報</h3>
    <table class="input">
      <tr><th>お部屋名称</th><td><%= session[:room_name]%></td></tr>
      <tr><th>宿泊日</th><td><%= session[:reserveDay]%></td></tr>
    </table><br />
    <h3>代表者情報</h3>
    <%= form_tag("/reserve/reserveCheck") do %>
      <table class="input">
        <tr>
          <th>代表者氏名（※）</th>
          <td><input type="text" name="name" value="<%=session[:name] %>">
              <% if @errmsg[:name]%>
              <span style ="color: red;"><%= @errmsg[:name]%></span>
              <%end%>
          </td></tr>
        <tr>
          <th>連絡先電話番号（※）</th>
          <td><input type="text" name="telno" value="<%= session[:telno]%>" />
              <% if @errmsg[:telno]%>
              <span style ="color: red;"><%= @errmsg[:telno]%></span>
              <%end%></td></tr>
        <tr>
          <th>メールアドレス</th>
          <td><input type="text" name="mail" value="<%= session[:mail]%>"></td></tr>
      </table><br />
    <h3>予約詳細情報</h3>
      <table class="input">
        <tr>
          <th>宿泊人数（※）</th>
        <td><input type="text" name="reserveNumber" value="<%= session[:reserveNumber]%>">
            <% if @errmsg[:reserveNumber]%>
            <span style ="color: red;"><%= @errmsg[:reserveNumber]%></span>
            <%end%>
        </td>
        </tr>
        <tr>
          <th>チェックイン予定時間（※）</th>
          <td><select name="checkin">
            <option value="">時間を選択</option>
            <option value="15:00">15:00</option>
            <option value="16:00">16:00</option>
            <option value="17:00">17:00</option>
            <option value="18:00">18:00</option>
            <option value="19:00">19:00</option>
          </select>
          <% if @errmsg[:checkin]%>
          <span style ="color: red;"><%= @errmsg[:checkin]%></span>
          <%end%></td>
        </tr>
        <tr>
          <th>連絡事項</th><td><textarea name="message" cols="40" rows="4" value="<%= session[:message]%>"></textarea></td>
        </tr>
      </table>
      <br>
    <p>まだ予約は確定しておりません。次の画面で確定させてください。</p>
    <input class="submit_a" type="submit" value="予約確認" />
    <input class="submit_a" type="button" value="前の画面に戻る" onclick="history.back();" />
    <%end%>
  </section>
      </article>
  </main>
